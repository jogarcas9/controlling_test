{
  "name": "controling_v2",
  "version": "1.0.0",
  "description": "Aplicación de finanzas personales",
  "main": "server/server.js",
  "engines": {
    "node": "22.x",
    "npm": "10.x"
  },
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client && npm install --legacy-peer-deps && npm run build",
    "server": "cd server && nodemon server.js",
    "client": "cd client && powershell -Command \"$env:NODE_OPTIONS='--openssl-legacy-provider'; npx react-scripts start\"",
    "dev:cmd": "start cmd /k \"cd server && npm run start\" && start cmd /k \"cd client && set NODE_OPTIONS=--openssl-legacy-provider && npx react-scripts start\"",
    "dev": "start powershell -ArgumentList \"-NoExit\", \"cd server; npm start\" && start powershell -ArgumentList \"-NoExit\", \"cd client; $env:NODE_OPTIONS='--openssl-legacy-provider'; npx react-scripts start\"",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install --legacy-peer-deps",
    "vercel-build": "npm run install-all && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.8.4",
    "concurrently": "^8.2.0",
    "recharts": "^2.15.3"
  },
  "devDependencies": {
    "@babel/plugin-proposal-optional-chaining": "^7.21.0",
    "@babel/preset-env": "^7.27.1",
    "@babel/preset-react": "^7.27.1",
    "@eslint/js": "^9.26.0",
    "eslint": "^9.26.0",
    "eslint-plugin-react": "^7.37.5",
    "globals": "^16.0.0",
    "nodemon": "^3.1.9"
  }
}
